{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the player window is allowed to ask the backend for.",
  "windows": ["main"],
  "permissions": ["core:default", "autostart:default"]
}
